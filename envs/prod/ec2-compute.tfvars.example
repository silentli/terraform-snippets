# Compute Layer - Production Environment
# EC2 instances and compute resources

# Project Configuration
project_name = "terraform-snippets"
environment  = "prod"

# Compute Configuration
ami_id         = "ami-0c02fb55956c7d316"  # Amazon Linux 2 AMI
instance_type  = "t3.medium"  # Production instance
key_name       = "your-key-pair-name"
instance_name  = "terraform-instance-prod"

# Network State (from bootstrap output)
network_state_bucket = "terraform-snippets-state-prod"
aws_region          = "us-east-1"
